## What & why

<!-- What this changes and why. Link the issue below. -->
Closes #

## Checklist
- [ ] Single, focused change (small PR)
- [ ] Migrations run clean (`cd backend && npm run migrate`) — if the schema changed
- [ ] Tests pass (`cd backend && npm test`)
- [ ] Screenshots for UI changes (dashboard / admin / menu)